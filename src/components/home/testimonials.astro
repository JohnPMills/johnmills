---
const { testimonials = [] } = Astro.props;
---

<style>
  .testimonial-scroll {
    animation: scroll 60s linear infinite; /* Slowed down from 30s to 60s */
  }
  
  @keyframes scroll {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }
  
  .testimonial-container:hover .testimonial-scroll {
    animation-play-state: paused;
  }
</style>

<div class="testimonial-container overflow-hidden py-8">
  <div class="max-w-7xl mx-auto px-6"> <!-- Added container with max-width and horizontal padding -->
    <div class="testimonial-scroll inline-flex gap-6 pl-[100%]"> <!-- Added left padding to create initial space -->
      {/* Duplicate testimonials for seamless looping */}
      {[...testimonials, ...testimonials].map((t) => (
        <div class="min-w-[300px] max-w-sm p-6 bg-white dark:bg-neutral-900 rounded-xl shadow text-left flex-shrink-0 mx-2"> <!-- Added horizontal margin -->
          <p class="text-sm text-neutral-700 dark:text-neutral-300 italic">"{t.quote}"</p>
          <p class="mt-4 font-semibold text-neutral-900 dark:text-white">— {t.name}</p>
          {t.title && <p class="text-xs text-neutral-500">{t.title}</p>}
        </div>
      ))}
    </div>
  </div>
</div>
